pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html chiroptera-tree.html public/
    - cp -r data public/
    - rm -rf public/data/raw
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
